1. 아나콘다 다운로드
2. 아나콘다 실행후 주피터->파이썬 켜기

* 영상처리 : 카메라, 스캐너등 영상 입출력 매체를 통해 받은 데이터로 
    유의미한 정보를 생성하는 기술
* 컴퓨터 비전 : 입력된 영상의 
* 영상처리의 한계점 : 
    1. 같은 물체더라도 사진이 다르면 다른물체로 인식
    2. 이미지만으로 판단 불가

OpenCV : 실시간 영상처리에 중점을 둔 영상처리 라이브러리
설치 방법
1. 설치
아나콘다 파워쉘에서
pip install opencv-python
확인 방법 아나콘다에서 import cv2 컴파일
2. 환경변수 설정

이미지 너비 높이 있음 (배열로 이루어져 있음)

색상 표현 정밀도 (비트)
=비트 깊이=색상 깊이=색상심도
    화소 하나의 색을 지시하기 위해 활용되는 비트 수
    정밀도가 높으면 처리 결과 정밀도도 높다 (정밀도가 높다고 정확도가 높은것은 아님)

색상 체널
   일반적으로 RGB+Alpha로 구성(4체널)
   Hue, Saturation, Value 채널 존재
   흑백 이미지 -> 1체널
    value0 검은색 255 흰색
ex) color = np.zeros((height,width,3),np.uint8)

ROI (Region Of Interest)

COI (channel Of Interest)

히스토그램

주로쓰는 파이썬 자료형 : 리스트, 튜플, 딕셔너리, 집합

리스트 : Container :  데이터 종류와 관계없이 넣을수 있음 , mutable : 생성 추가 삭제 변경 , sequence : index를 통해 접근 능